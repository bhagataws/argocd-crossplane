apiVersion: v2
name: crossplane
description: Deploys Crossplane via the official Helm chart
type: application
version: 0.1.0
appVersion: "2.4.0"
preferredNamespace: crossplane
dependencies:
  - name: crossplane
    version: "2.4.0"
    repository: "https://charts.crossplane.io/stable"
